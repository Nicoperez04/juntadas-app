import React from 'react';
import { AppNavigator } from '@/navigation/AppNavigator';

// Este archivo es el encargado de manejar la navegación de la app, se encarga de mostrar el AppNavigator.

export default function App() {
  return <AppNavigator />;
}